{
  "name": "onebun-future-example",
  "version": "0.1.0",
  "description": "Example application (concept) using future version of OneBun framework",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@onebun/core": "workspace:*",
    "@onebun/envs": "workspace:*",
    "@onebun/logger": "workspace:*",
    "@onebun/metrics": "workspace:*",
    "@onebun/requests": "workspace:*",
    "@onebun/trace": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "1.2.2",
    "@types/node": "^22.9.0"
  }
}
